// Array printing example
let numbers = [1, 2, 3, 4, 5;  // BUG: missing closing bracket

function printNumbers(arr) {
    for (let i = 0; i < arr.length; i++) {
        console.log(arr[i]);
    }
}

printNumbers(numbers);
